# Reliable AI Agents

AI agents combine a model with tools, state, planning, and feedback loops. Reliability improves when the system retrieves trusted context, validates tool inputs, limits permissions, records sources, and evaluates outputs. Retrieval-Augmented Generation can reduce unsupported claims by giving the model relevant source material before answer generation. Human approval should be used for high-impact actions.
